import { getAll } from "../server/products.mjs"
import produits from "../server/products.json"

//Modifier le chemin du fichier dans "loadProducts()" pour effectuer le test
/*describe("test des produits", () => {
    test("list des produits en stock", () => {
        expect(getAll()).toMatchObject(produits)
    })
})*/